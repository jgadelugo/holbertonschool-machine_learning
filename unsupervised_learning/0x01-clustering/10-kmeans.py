#!/usr/bin/env python3
"""performs K-means on a dataset"""
import sklearn.cluster


def kmeans(X, k):
    """performs K-means on a dataset
    @X: np.ndarray shape(n, d) data set
    """
    return None, None
