#!/usr/bin/env python3
"""creates input for the generator"""
import tensorflow as tf
import numpy as np


def sample_Z(m, n):
    """creates input for the generator
    @m: number of samples that should be generated
    @n: number of dimensions of each sample
    * All samples should be taken from a random uniform distribution
    within the range [-1, 1]
    Return: Z
        @Z = np.ndarray shape (m, n) containing the uniform samples
    """
    Z = None

    return Z
